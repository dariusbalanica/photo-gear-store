version: "2"
services:
  server:
    build: ./server
    links:
      - db
    depends_on:
      - db
    ports:
      - "5000:5000"
    tty: true
    stdin_open: true

  client:
    build: ./client
    links:
      - server
    depends_on:
      - db
    tty: true
    stdin_open: true

  administration:
    build: ./administration
    links:
      - db
    depends_on:
      - db
    tty: true
    stdin_open: true

  db:
    image: mysql:5.7
    ports:
      - "32000:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./db:/docker-entrypoint-initdb.d/:ro
